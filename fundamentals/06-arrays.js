let my_arr = [100, 200, 300];

console.log(my_arr);
console.log(my_arr.length);

console.log(my_arr.at(1));

my_arr.push(400);

console.log(my_arr.length);

/// Arrays challange:
/// Define an empty array
/// Push some integers
/// Console log it
/// Console log the length of it
/// Console log the first element
